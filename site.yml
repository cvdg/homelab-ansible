---
- name: Update package cache
  hosts: all, !srv01.griend.dev
  become: true
  tags: always
  tasks:
    - name: Update package cache
      ansible.builtin.apt:
        update_cache: true
      changed_when: false

- name: Configure all
  hosts: all, !srv01.griend.dev
  become: true
  tags: always
  roles:
    - users
    - common

- name: Configure dns
  hosts: dns
  become: true
  tags: dns
  roles:
    - dns

- name: Configure servers
  hosts: servers, !srv01.griend.dev
  become: true
  tags: servers
  roles:
    - kvm
    - terraform
